# socketio_example
A following of the Socket.io tutorials, plus sandbox usage afterwards


## TODOs: First Homework 
- Broadcast message to connected users that a user has disconnected
- Don’t send the same message to the user that sent it. Instead, append the message directly as soon as they press enter
- Add '(user) is typing' functionality
- Show who's online
- Add private messaging
